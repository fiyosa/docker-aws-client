version: '3'

services:
  test-client:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: test-client
    ports:
      - 3001:3001
    environment:
      NODE_ENV: development
